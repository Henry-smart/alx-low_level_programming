#include <stdio.h>

/**
 * main - prints alphabet in lower case
 * Return: Always 0
 */
int main(void)
{
	char Az;

	for (Az = 'a'; Az <= 'z'; Az++)
	{
		putchar(Az);
	}

	putchar('\n');

	return (0);
}
